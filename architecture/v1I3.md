# Parking Reservation System - Frontend Components

## 📋 Vue d'ensemble

Ce projet contient les composants React pour la page principale de réservation de parking. L'interface permet aux employés de visualiser le plan du parking, sélectionner une place et effectuer une réservation.

## 🎨 Composants

### 1. ParkingReservationPage (Principal)
Composant principal qui orchestre tous les autres composants. Gère l'état global de l'application et la communication avec l'API.

**Fonctionnalités:**
- Chargement des réservations existantes
- Gestion de la sélection de place
- Création et annulation de réservations
- Authentification utilisateur

### 2. ParkingMap
Affiche le plan complet du parking avec 60 places (6 rangées × 10 places).

**Fonctionnalités:**
- Visualisation interactive du parking
- Indication visuelle des places disponibles/réservées
- Filtrage des places avec bornes électriques (rangées A et F)
- Sélection de place au clic
- Légende explicative

**Particularités:**
- Les rangées A et F ont des bornes électriques (indiquées par ⚡)
- Layout responsive avec séparateurs entre paires de rangées
- Indicateurs d'entrée et sortie

### 3. ParkingSpot
Composant individuel représentant une place de parking.

**États possibles:**
- **Available** (blanc/bleu): Place disponible
- **Reserved** (rouge): Place déjà réservée
- **Selected** (bleu foncé): Place sélectionnée par l'utilisateur
- **Unavailable** (gris): Place non disponible selon les filtres

**Indicateurs:**
- Badge électrique (⚡) pour les places avec borne

### 4. ReservationPanel
Panneau de contrôle pour créer une nouvelle réservation.

**Fonctionnalités:**
- Sélection de dates (début et fin)
- Option "Besoin d'une borne électrique"
- Validation des contraintes:
  - Maximum 5 jours pour employés (30 pour managers)
  - Date de début ≥ aujourd'hui
  - Date de fin ≥ date de début
- Affichage de la place sélectionnée
- Bouton de réservation

### 5. UserReservations
Liste des réservations actives de l'utilisateur.

**Fonctionnalités:**
- Affichage de toutes les réservations
- Indicateur de check-in effectué
- Alerte pour check-in requis (avant 11h)
- Bouton check-in pour les réservations du jour
- Bouton annulation pour les réservations futures
- Badge "Aujourd'hui" avec rappel

## 🚀 Installation

### Prérequis
- Node.js 16+
- npm ou yarn

### Installation des dépendances

```bash
npm install react react-dom
```

### Structure des fichiers

```
src/
├── components/
│   ├── ParkingReservationPage.jsx
│   ├── ParkingReservationPage.css
│   ├── ParkingMap.jsx
│   ├── ParkingMap.css
│   ├── ParkingSpot.jsx
│   ├── ParkingSpot.css
│   ├── ReservationPanel.jsx
│   ├── ReservationPanel.css
│   ├── UserReservations.jsx
│   └── UserReservations.css
└── App.jsx
```

## 💻 Utilisation

### Intégration dans votre application

```jsx
import React from 'react';
import ParkingReservationPage from './components/ParkingReservationPage';

function App() {
  return (
    <div className="App">
      <ParkingReservationPage />
    </div>
  );
}

export default App;
```

## 🔌 API Integration

Les composants sont prêts pour l'intégration API. Voici les endpoints à implémenter:

### GET /api/reservations
Récupère les réservations pour une période donnée.

**Query params:**
- `startDate`: Date de début (YYYY-MM-DD)
- `endDate`: Date de fin (YYYY-MM-DD)

**Response:**
```json
[
  {
    "spotId": "A01",
    "date": "2026-02-03",
    "userId": "user123",
    "checkedIn": true
  }
]
```

### GET /api/user/reservations
Récupère les réservations de l'utilisateur actuel.

**Response:**
```json
[
  {
    "id": "1",
    "spotId": "A01",
    "startDate": "2026-02-03",
    "endDate": "2026-02-05",
    "checkedIn": false
  }
]
```

### POST /api/reservations
Crée une nouvelle réservation.

**Body:**
```json
{
  "spotId": "B05",
  "startDate": "2026-02-04",
  "endDate": "2026-02-06"
}
```

### DELETE /api/reservations/:id
Annule une réservation.

### POST /api/reservations/:id/checkin
Effectue le check-in pour une réservation.

## 🎨 Personnalisation

### Variables CSS

Les couleurs et styles sont définis dans `ParkingReservationPage.css` via des variables CSS:

```css
:root {
  --primary-color: #2563eb;
  --primary-dark: #1e40af;
  --success-color: #10b981;
  --warning-color: #f59e0b;
  --danger-color: #ef4444;
  /* ... */
}
```

Modifiez ces variables pour adapter le thème à votre charte graphique.

### Police de caractères

Le projet utilise **Plus Jakarta Sans** de Google Fonts. Vous pouvez la changer dans `ParkingReservationPage.css`:

```css
@import url('https://fonts.googleapis.com/css2?family=VotrePolice:wght@300;400;500;600;700&display=swap');
```

## 📱 Responsive Design

L'interface est entièrement responsive avec 3 breakpoints:

- **Desktop**: > 1200px - Layout en 2 colonnes
- **Tablet**: 768px - 1200px - Layout adaptatif
- **Mobile**: < 768px - Layout en colonne simple

## ✨ Fonctionnalités clés

### Contraintes respectées
- ✅ Maximum 5 jours de réservation (employés) / 30 jours (managers)
- ✅ Réservation dès aujourd'hui si place disponible
- ✅ Filtrage des places électriques (rangées A et F)
- ✅ Indicateur de check-in requis avant 11h
- ✅ Libération automatique si pas de check-in
- ✅ Historique complet des réservations
- ✅ Interface adaptée aux profils (employé/secrétaire/manager)

### UX améliorée
- Feedback visuel immédiat sur les interactions
- Animations fluides et professionnelles
- Messages d'erreur clairs
- États de chargement
- Confirmations pour actions critiques

## 🔒 TODO - Intégrations futures

1. **Authentification**
   - Intégrer JWT ou OAuth
   - Gérer les rôles (employé/secrétaire/manager)

2. **Check-in via QR Code**
   - Générer QR codes pour chaque place
   - Scanner et valider via mobile

3. **Notifications**
   - Email de confirmation après réservation
   - Rappel check-in le matin
   - Alerte libération automatique

4. **Dashboard Manager**
   - Statistiques d'utilisation
   - Taux d'occupation
   - No-show analytics

5. **Backend complet**
   - API REST ou GraphQL
   - Base de données
   - File d'attente pour emails

## 🧪 Tests

Pour ajouter des tests:

```bash
npm install --save-dev @testing-library/react @testing-library/jest-dom
```

Exemple de test pour ParkingSpot:

```jsx
import { render, screen } from '@testing-library/react';
import ParkingSpot from './ParkingSpot';

test('affiche le numéro de place', () => {
  render(<ParkingSpot spotId="A01" isAvailable={true} />);
  expect(screen.getByText('A01')).toBeInTheDocument();
});
```

## 📄 Licence

Ce code fait partie du projet de réservation de parking pour le cours de Software Design.

## 🤝 Contribution

Pour contribuer:
1. Créer une branche feature
2. Implémenter les changements
3. Ajouter/mettre à jour les tests
4. Créer une pull request

## 📞 Support

Pour toute question sur l'implémentation, contactez l'équipe de développement.

---

**Version:** 3.0 (Itération 3)
**Dernière mise à jour:** Février 2026